# Other special folders
set mbox = "+math/INBOX"
set postponed = "+math/Drafts"

# msmtp -----------------------------------
set from = "robert@math.aau.dk"
set realname = "Robert Dahl Jacobsen"
set sendmail = "/usr/local/bin/msmtp -a math"
set sendmail_wait = 0
set record=+math/"Sent Items"

color status cyan default

macro index D \
    "<save-message>+math/Trash<enter>" \
    "move message to the trash"

